services:
  - type: web
    name: cat-grooming-hr
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn cat_hr.wsgi:application --bind 0.0.0.0:$PORT
